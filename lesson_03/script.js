const colors = ['red', 'green', 'blue'];

console.log(colors.length);

colors[0] = 'pink';

colors.pop();
colors.shift();

console.log(colors);

colors.push('yellow');
colors.unshift('purple');

console.log(colors);
